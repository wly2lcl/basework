# RUN-003：CLI/TUI 统一接入服务
> **2026-09-16 再复审：下文完成结论均为历史记录。** 前置任务回退；本任务既有实现和局部证据保留，等待依赖修复后回验，不要求重写模块。 当前状态仅见 [TASKS](../../TASKS.md)，依据见 [本次复审](REVIEW-2026-09-16.md)。

> **2026-09-14 复审说明**：以下为历史实施记录，不能继续单独支撑当前验收。新发现或依赖回退涉及 A02、A09、A12；详见 [本轮复审报告](REVIEW-2026-09-14.md) 与 [任务卡](../../tasks/06-runtime.md#run-003) 的复审补充。实际状态只维护在 [TASKS](../../TASKS.md)。旧结论保留用于追溯，本轮未修业务代码。

## 基线

- 前置 RUN-002 已完成（同日）：`internal/runtime.Service` 契约 + `LocalService`（串行排队、有界订阅、关闭取消）。
- 改造前的运行路径：CLI（`agent -m` 一次性、REPL）与 TUI（`tui.go` 的 `SetInputHandler`）都直接调 `agt.HandleMessage`；TUI 的流式回调在 Agent 构造时一次性注册（`agent.WithCallback(opts.Callback)`），与运行无关——理论上一次被取消运行的收尾增量可以落进下一次运行的界面。
- 退出路径：CLI/TUI 各自 `defer agt.Close()`，服务的资源（进行中运行、任务管理器）不经由统一入口释放。

## 变更

- **`internal/runtime/callback.go`（新增）**：`CallbackSwitch`——按运行绑定的回调路由器（实现 `agent.Callback`）。Agent 注册的永远是路由器；每次运行绑入当次 UI 回调、结束（成功/取消/报错）立即解绑，解绑后到达的事件（迟到的 flush）一律丢弃。`Unbind(cb)` 只在当前绑定与传入者一致时清除，防误清。
- **`internal/runtime/service.go`**：
  - `LocalDeps` 增 `CallbackSwitch`、`UICallback`；
  - 新增 `StartWithCallback(ctx, input, cb)`（cb 非 nil 时覆盖 UICallback）；`Start` 委托它自动绑入 UICallback；绑定/解绑以 defer 挂在运行的完整生命周期上；
  - `RunEvent` 增 `SessionID`（订阅按会话路由的依据）。
- **`cmd/basework/runtime.go`**：`newRuntimeAgent` 无条件注册 `CallbackSwitch` 为 Agent 回调，`opts.Callback` 改存为 `rt.uiCallback`；`Service()` 把两者传入 `LocalDeps`。
- **`cmd/basework/agent.go`**：one-shot 与 REPL 改走 `rt.Service().Start`；取消语义统一为 `Run.Err == context.Canceled`（REPL 的「[响应被中断]」判断加 `errors.Is`）；退出路径从 `defer agt.Close()` 改为 `defer svc.Close()`（服务负责运行取消→Agent→任务管理器的顺序释放）。
- **`cmd/basework/tui.go`**：`--no-tui` 与 TUI 主分支同样走服务；TUI 的 input handler 调 `svc.Start`；`runTUI` 增加订阅转发 goroutine（RunEvent → `tui.RunEventMsg`），`defer sub.Unsubscribe()` 覆盖全部退出路径。
- **`internal/tui/app.go`**：新增 `RunEventMsg`（RunID/SessionID/Kind/Err）；`App.SessionID` 字段；`Update` 按会话路由——不匹配的事件整条忽略（`lastRunEvent` 作为测试观察点，UI 呈现留给 UI-001）。

## 验证

- 新增测试：
  - `internal/runtime`：`TestStartWithCallback_BindsDuringRun_DropsAfterFinish`（结束后迟到增量丢弃）、`TestStartWithCallback_ForwardsDuringRun`（运行中转发、解绑后丢弃）、`TestStart_AutoBindsUICallback`（UICallback 自动绑定）、`TestRunEvent_CarriesSessionID`（事件携带会话 ID）——4 条全过。
  - `internal/tui`：`TestApp_Update_RunEventRouting`（本会话接受/他会话忽略/未绑定全接受）——通过。
  - `cmd/basework`：`TestRuntimeAgentServiceCallbackWiring`（TUI 带回调与 CLI 无回调两种装配都可运行）——通过。
- 任务卡针对性验证：`go test -race ./cmd/basework ./internal/tui ./internal/runtime -count=1` → 3 包全部 ok。
- 全量门禁：`go build -tags "sqlite memory" ./...` + `go vet -tags "sqlite memory" ./...` 通过；`GOOS=windows`/`GOOS=linux go build ./...` 通过；双模式全量测试 34/34 包 ok（退出码均 0）；`gofmt -l` 干净；`make check-arch` 通过；`make gen` 幂等。
- 两入口实测（隔离 HOME，真实二进制）：
  - `agent -m "hello"`（无 key）：请求穿透服务链路到达 provider（400），错误以统一的「处理消息失败」返回，退出码 1，干净退出；
  - `agent` REPL + `exit`：退出码 0；
  - `tui --no-tui` + `exit`：退出码 0。
- TUI 全功能（bubbletea 渲染）无法在无 TTY 的沙箱中实测：交互部分以 `--no-tui` 回退路径与单元测试覆盖；全屏 TUI 的人工验证属 UI 系列任务（UI-001 起）。

## 剩余与交接

- 覆盖边界：会话切换的 UI 能力本身尚未存在（UI-003 交付）；本任务交付的是其路由基础（事件带 SessionID、App 按会话过滤、回调按运行隔离）。
- `RunEventMsg` 目前只做路由与记录（`lastRunEvent`），状态卡片展示由 UI-001 接手。
- TUI 全屏模式的人工实测需真实 TTY，随 UI 系列任务一起做。

## 结论

RUN-003 验收条件逐项满足：CLI/TUI 取消和错误语义一致（同一 `Run.Err` 通道）；旧任务回调不串到新会话（CallbackSwitch 按运行绑定+解绑，机制性保证而非约定）；所有关闭路径回收服务订阅（`defer svc.Close()` + `defer sub.Unsubscribe()` 覆盖退出/错误/取消路径）。任务完成。

## 2026-09-15 修复复核

继续收口 A02/A09/A12：主 Agent 装配已实际追加压缩器、循环检测、事件总线和子代理
行为选项；CLI、`--no-tui` 与 TUI 的取消都在检查 `context.Canceled` 后再决定展示
中断状态。TUI 当前轮支持 Ctrl+C/Ctrl+D 取消并留在界面，切换会话会取消本轮、
增加 generation，迟到最终响应不会写入新会话。

运行回调增加可选 `ForRun` 作用域，流式/思考/工具/错误消息带 `RunID` 与 `SessionID`；
App 按会话和活动运行过滤异步消息，旧会话迟到消息不会污染新界面。新增 TUI 取消、
切换与旧响应隔离用例；`go test -tags "sqlite memory" -race ./...`、`go vet` 与
完整构建通过。

仍待真实 TTY 及真实 Provider 的三入口人工验收，故看板保留 RUN-003“进行中”。

## 2026-09-15 产品入口复核

当前 checkout 已用真实二进制、真实 PTY 和本地 OpenAI 兼容假服务跑通 scenario1–4，
并用 `--no-tui`/CLI 入口的既有测试覆盖普通错误、取消和关闭。scenario1 验证了主
Agent 的运行行为接线、流式工具卡片和 Ctrl+C 退出；scenario2 验证了后台任务卡片、
取消与进程组清理；scenario3 验证同一 session ID 重启继续和旧回调隔离；scenario4
验证模型切换与缺 key 错误。完整输出见 [QA-001](QA-001.md)。

因此 RUN-003 的原验收条件已满足，当前看板改为“完成”。真实外部 Provider 的成本、
模型差异和真人体验仍属于发布验收边界，不用脚本化 Provider 冒充。

## 2026-09-16 再复审交接

前置任务回退；本任务既有实现和局部证据保留，等待依赖修复后回验，不要求重写模块。 本次状态调整为待验证，小步骤见原任务卡新增补充。历史成功测试不删除，但不能代替本次缺陷修复后的验证。
