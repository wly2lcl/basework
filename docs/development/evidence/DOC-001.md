# DOC-001 文档集中验证记录

## 基线

- 日期：2026-09-12；操作者：Codex。
- 环境：macOS / Darwin 25.6.0 arm64；Go 1.26.0。
- HEAD：`6f70691`；本轮开始前已有未提交的 session、agent、compaction、文档门禁和生成器改动。
- 范围：文档整理、文档检查器与必要的生成器引用/Makefile 入口。不实施后续业务任务，不以此前工作区改动作为本轮新功能。

## 变更

- 根目录 README 缩为导航；架构、贡献、安全、行为准则和变更日志正文迁入 docs。
- 12 个 pkg 契约集中到 reference/pkg，增加 Model Experience，并修正请求指纹的实际限制。
- 旧任务、路线图、状态、介绍、设计分册与研究方案归档，保留原历史文字和勾选状态；内部链接迁移。
- 新建 M0–M8 路线图与 28 个任务，包含依赖、步骤、验收、验证命令和证据回填要求。
- 新增 AI 开发/交接指令、验证规则和证据模板；progress 直接读取唯一看板。
- 检查器新增集中布局、任务 ID/依赖/状态/锚点/证据规则，测试包含故意损坏的反向样例。

## 验证

| 检查 | 命令或操作 | 实际结果 | 边界 |
|---|---|---|---|
| 检查器与生成器测试 | `go test ./scripts/doccheck ./scripts/gendeps ./scripts/docstats -count=1` | 通过 | 测试覆盖重复 ID、无效依赖、循环依赖、非法状态、缺负责人、错误锚点、缺证据、布局回流、缺契约小节和死链 |
| 静态检查 | `go vet ./scripts/doccheck ./scripts/gendeps ./scripts/docstats` | 通过 | 仅本轮相关脚本 |
| 文档与任务进度 | `make check-docs`、`make progress` | 通过：81 个 Markdown、12 个包契约、28 个任务 | 仅 DOC-001 完成，下一项 BASE-001 |
| 生成物确定性 | 连续两次 `make gen` 后比对 SHA-256 | DEPGRAPH/STATS 一致 | 不把覆盖率写入确定性生成物 |
| 差异格式 | `git diff --check` | 通过 | 不代表业务验收 |
| 布局核查 | 枚举根目录与 pkg Markdown | 根目录仅 README；pkg 中为 0 | GitHub 表单模板保留平台固定位置 |
| 架构边界 | `go test ./tests -run TestArch -count=1` | 通过 | 不代替业务全量测试 |

执行时使用 `/private/tmp/basework-gocache` 作为可写 Go 缓存；仓库的标准命令不依赖该路径。

## 剩余与交接

- 本轮未重跑全部业务测试、真实模型调用、人工 TUI 或跨平台安装；这些工作由 BASE/REL/SHIP 任务明确跟踪。
- 根目录和源码包旧文档路径不再保留正文；外部旧书签需改为 docs 中的新位置。
- 自动校验不检查外部网页可用性，也不证明证据文字真实；普通文档校验相对文件链接，任务卡另外校验显式 ID 锚点。
- 下一项：BASE-001，建立当前工作区完整验证基线。后续 27 项保持待办。

## 结论

DOC-001 的文档迁移、集中包契约、任务依赖和证据校验要求均已满足，标为完成。其余 27 项为后续工作，不计入本轮已实现业务能力。
