# OPT-003：长会话追加优化交接

## 基线

来自 [OPT-002](OPT-002.md) 的三次原始样本：10000 事件追加 112–130 秒、约 8 GB 累计分配。不是本次重新测量；当前 CLI 仍使用 JSONL。

## 变更

本次只建立可执行任务与验收目标，未改存储代码。实施卡见 [OPT-003](../../tasks/09-follow-up.md#opt-003)。

## 验证

尚未执行优化或新基准。原始基线链接见上文；新增目标是待验证指标，不是当前性能声明。

## 剩余与交接

先跑同环境 JSONL/SQLite 对照、记录完整快照大小和 profile；确认序号/锁/恢复不变量后选择方案。一次只交付基线、实现、正确性回归、性能验收中的一个切片。

## 结论

属于新增 P3 优化，状态仅见 [TASKS](../../TASKS.md)。优先完成发布门禁缺陷，不用性能工作替代正确性修复。
