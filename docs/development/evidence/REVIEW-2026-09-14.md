# 2026-09-14 计划完成度复审

## 基线

- 审查对象：`bb455ff93bbb`（开始时工作区干净），对照 M0–M8 的 29 张任务卡、证据、产品入口与测试。
- 环境：macOS / darwin arm64、Go 1.26.0；本轮不调用付费模型，不发布、不提交 Git，只更新文档。
- 方法：代码调用链核对、默认/完整构建测试、扩大范围的 race、隔离临时目录中的 Go overlay 复现、实际 CLI help、全新目录中的 PTY 入口检查。
- 优先级：P1 为数据/权限/恢复或核心运行问题，先修再验收；P2 为交互正确性、资源边界或证据不足；P3 为后续体验/性能优化。
- 这是一次有范围的审查，不是全部并发时序、所有 Provider 或全部平台的正确性证明。

## 变更

已有的实现值得保留：job 状态机与 owner 隔离、输出溢出、编辑预览与提交前基线检查、精确压缩快照与请求审计、配置预设与资源释放、UI 组件和协议回归都有实际代码和测试。

问题集中在**组件之间的接线与最终验收**。原看板 29/29 完成不能成立：本轮保留 15 项既有验收，14 项因直接缺陷或依赖回退重新打开；另拆出 QA-001、OPT-001、OPT-002 三项。此计数是本次审查快照，不代表只写完一半代码；持续进度只看 [TASKS](../../TASKS.md)。

本轮不修业务代码。以下复现失败表示“确认现有缺陷”，不是已经修复。

## 验证

### 既有门禁

| 检查 | 本轮结果 | 能证明的范围 |
|---|---|---|
| `go test -timeout=3m -count=1 ./...` | 通过 | 默认构建现有测试 |
| `go test -tags "sqlite memory" -timeout=3m -count=1 ./...` | 通过 | 完整构建现有测试 |
| `go test -tags "sqlite memory" -race -timeout=5m -count=1 ./pkg/... ./internal/permission ./internal/tui ./cmd/basework ./internal/runtime ./internal/jobs ./internal/edits` | 通过 | CI 原范围，加 runtime/jobs/edits；不包括新复现探针 |
| `go vet -tags "sqlite memory" ./...`、完整 CLI 构建 | 通过 | 静态检查与本机可运行产物 |
| `make check-arch`、`make gen` 后生成物差异 | 通过、无差异 | 分层、生成物新鲜度 |
| 本轮文档最终检查 | 见本文件末尾 | 布局/链接/任务依赖，不证明语义 |
| 既有跨平台 CI | 只读实时核验 success | [run 34797549128](https://github.com/wly2lcl/basework/actions/runs/34797549128)，head 为 `84a39e4c83ce9a5fffe245e6e0d9702d9e5e3d01`，不是本次文档变更后的 commit |

首轮测试被沙箱禁止监听 loopback 端口中断；允许本地测试服务后，上述三组测试全部退出 0。该首轮失败归类为环境限制，不能作为产品缺陷。

### 发现与处理入口

| 编号 / 优先级 | 触发、实际结果与影响 | 代码入口 | 回填任务 |
|---|---|---|---|
| A01 / P1 | 提交后撤销：权限已撤回仍写回；父目录换成指向工作区外的软链且外部文件内容等于提交结果时，撤销会改写外部文件。两个隔离目录复现均成立 | `internal/edits/commit.go:448`，Rollback 未经过 Planner.ResolvePath | EDIT-002、EDIT-003 |
| A02 / P1 | 主 runtime 构造没有调用 `runtimeBehaviorOptions`。启用压缩后 Agent 的 compactor 仍为 nil，默认循环检测器也为 nil；同一遗漏还影响事件总线与 sub-agent runner 注入 | `cmd/basework/runtime.go:192`、`:491` | RUN-003、CTX-003 |
| A03 / P1 | 重新启动创建新 session ID；ReconcileOwner 只检查新 owner，旧 running job 保持 running。CLI/TUI 没有选择旧会话继续的入口。存储投影可恢复不等于产品能继续旧会话 | `pkg/agent/loop.go:136`、`cmd/basework/runtime.go:603`、`cmd/basework/tui.go` | CTX-003、UI-003、JOB-004 |
| A04 / P1 | 实际 edit_files 预览→提交→关闭 runtime 后，持久化 WorkspaceFacts 仍为 0。SaveWorkspaceFacts 在非测试代码中只有定义，没有调用方；facts show 只做临时聚合。默认新用户开启摘要也读不到这些事实 | `cmd/basework/facts.go`、`runtime.go:870`、`pkg/session/facts.go:160` | CTX-001、CTX-002 |
| A05 / P1 | Start 等锁前检查 closed，等锁后不再检查；关闭之后排队调用仍进入 Agent。Close 后 Subscribe 得到永不自动关闭的通道；Close 未等待运行退出即释放资源 | `internal/runtime/service.go:178`、`:248`、`:259` | RUN-002 |
| A06 / P1 | 日志容忍最后半行，但 Append 直接接在半行后面写新 JSON，下一次读取变成中间行损坏。恢复或运行新任务可使 jobs 历史不可读 | `internal/jobs/journal.go:100`、`:144` | JOB-004 |
| A07 / P2 | 同一 facts Provider 两次 Collect，文件哈希改变仍不标过期：每次新建 tracker；产品仅启动时 Collect 一次。预算 80 字节实际输出 183 字节（标题与截断说明未计入）；相对 Windows 路径 `..\\outside.txt` 仍被传给 hash 回调 | `pkg/agent/facts_provider.go:55`、`:82`、`pkg/session/facts_summary.go:86` | CTX-002 |
| A08 / P2 | 批量编辑部分成功时 phase=failed；FoldFileEdited 要求 phase=committed，丢掉实际 state=written 的文件。复现写成功 1 个文件却得到 0 条事实 | `internal/tools/edit_files.go:272`、`pkg/session/facts.go:245` | CTX-001、EDIT-003 |
| A09 / P2 | SwitchSession 不清 Messages/IsStreaming；迟到 AgentResponseMsg 仍写入新会话。流式、工具、错误与审批消息缺少完整 session/run 标识；job 读/取消闭包仍绑定旧 owner。产品本身也没有切换命令 | `internal/tui/app.go:306`、`:322`、`:697`、`callback.go` | RUN-003、UI-003 |
| A10 / P2 | 一次读回 60 行（不足 4KB），只显示前 30 行；按下一页因 EOF 拒绝，31–60 行永远看不到。恢复面板宽度 11–23 时使用负长度切片；宽度 20 实测 panic | `internal/tui/jobs.go:199`、`:253`、`internal/tui/resume.go:61` | UI-001、UI-003 |
| A11 / P2 | edit_files commit 参数只有 action/plan_id；审批适配器不查真实计划，弹窗 paths=[]、diff=""。preview 的模型参数展示不是 commit 时与磁盘一致的可信差异 | `internal/permission/approval.go:188`、`:205` | UI-002、EDIT-003 |
| A12 / P2 | TUI 全局 Ctrl+C/Ctrl+D 直接退出，未实现“取消本轮后留在界面继续”；无 TUI REPL 在检查错误前先 cancel，所有普通错误也会被判为“响应被中断”。此项为代码路径核对，未声称已做完整 PTY 取消复现 | `internal/tui/app.go:440`、`cmd/basework/tui.go:296` | RUN-003、UI-003 |
| A13 / P2 | 全新 SHIP003_ROOT 按文档运行 scenario1，立即缺 fixtures；假服务也从临时根读取，GO_ROOT 硬编码；scenario5 的 examples 路径少一级。examples/embed 依赖 internal/runtime，不能作为外部 Go module 的公共 API 示例；eventsSeen 用 sleep 等待而无同步 | `tests/tui_pty/harness.py`、`scenario5.py:25`、`examples/embed/main.go` | QA-001、SHIP-003 |
| A14 / P2 | PTY scenario3 以“切换到会话”通用提示也算历史恢复，S3R 假服务不验证旧历史就固定返回“已看到事实”；安装只验证旧 darwin/arm64 快照，CI dry-run 跳过 Docker。历史“全部完成”与任务原验收条件不符 | `tests/tui_pty/scenario3.py`、`fake_openai.py`、`docs/development/evidence/SHIP-002.md` | QA-001、SHIP-001/002/003 |

A07 的 Windows 路径结论分两层：本机验证的是相对反斜杠路径未被拦截；Windows 实际文件读取越界仍需在目标平台隔离夹具中验证。修复必须同时覆盖盘符相对路径、根路径、UNC、两种分隔符和软链，不能仅加一个字符串判断。低预算越界已经本机复现；文件哈希目前整文件读取，64 个文件的次数上限也不能替代单文件字节上限。

### 可重复的复现

[最小复现与执行方法](../../development/review-reproduction.md) 保存本轮 16 个测试的完整源码，全部以“期望正确行为”为断言，当前基线全部失败。通过 `go test -overlay` 加载，业务源码和常规测试未修改；修复时应把对应测试转为包内正式回归，并补完整产品入口验收。

另做了两项实际入口检查：

- 当前 CLI help：agent 没有 `--model`，tui 只有 `--no-tui` / `--preset` 等帮助中列出的参数，session 只有 list/clear/status/unlock。旧指南中的 resume/export/search 和 TUI --theme 等不能继续作为可执行操作。
- `SHIP003_ROOT=<全新目录> python3 tests/tui_pty/scenario1.py` 退出 1，`FileNotFoundError: <全新目录>/fixtures`。未继续把手工补目录后的运行称为“按文档通过”，也未重跑真实模型。

历史 REL-003/SHIP-001 的真实模型结果保留为对应日期、指定模型与旧工作区的历史证据。本轮未验证其临时原始日志，也不能把它们改称当前候选版本的成功率。

## 剩余与交接

1. 先完成 EDIT-002（撤销边界）与 JOB-004（残尾日志），再完成依赖 JOB-004 的 RUN-002（关闭/排队）；一次一个任务。
2. RUN-003 修主入口行为注入；CTX-001/002 接通真实事实与摘要，再由 CTX-003 接通指定会话恢复和双压缩重启。
3. UI-001/002/003 修分页、审批与所有异步消息路由，使用已修复的产品入口验收。
4. QA-001 保存干净可复跑的 PTY/真实模型夹具，强化恢复断言；SHIP-001/002/003 在同一候选代码与产物上重新验收。
5. OPT-001/002 分别改善非交互初始化与性能基线，优先级低于上述缺陷；不先扩展远程服务、工作流或更多客户端。

所有小步骤、预期结果、范围与依赖已写回 [任务卡](../../TASKS.md)。旧证据保留，并在受影响文件顶部标出本次复审说明，避免历史结论覆盖当前判断。

## 结论

当前定位是“核心实现与常规回归已具备，产品闭环仍有缺陷”，不具备宣称 M0–M8 全部验收完成的依据。保留已有成果，修复具体失败场景后逐项恢复验收；不能通过删断言或修改验收措辞让完成率重新变成 100%。

本轮最终文档验证：`make check-docs progress` 退出 0，117 份 Markdown 的结构、链接和任务依赖通过，任务数量为 15/32 完成；`git diff --check` 通过。变更范围为 42 份 `docs/` 下的 Markdown，业务源码、常规测试与生成物未修改。

按新文档重新汇总运行 16 个探针的提权请求，被自动审批复核因用量额度限制拒绝，未执行。此前 13 个组件探针与 3 个 runtime 探针已分别执行，全部按预期暴露缺陷；文档中 8 份 Go 源码与已执行版本的正文逐字一致（忽略首尾空白），汇总脚本的 Python 语法检查通过。未绕过该拒绝，也不将汇总脚本记为已运行通过。
