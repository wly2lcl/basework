# UI-003 会话切换与可恢复进度
> **2026-09-16 再复审：下文完成结论均为历史记录。** 前置任务回退；本任务既有实现和局部证据保留，等待依赖修复后回验，不要求重写模块。 当前状态仅见 [TASKS](../../TASKS.md)，依据见 [本次复审](REVIEW-2026-09-16.md)。

> **2026-09-14 复审说明**：以下为历史实施记录，不能继续单独支撑当前验收。新发现或依赖回退涉及 A03、A09、A10、A12；详见 [本轮复审报告](REVIEW-2026-09-14.md) 与 [任务卡](../../tasks/07-terminal.md#ui-003) 的复审补充。实际状态只维护在 [TASKS](../../TASKS.md)。旧结论保留用于追溯，本轮未修业务代码。

## 基线

- 前置：UI-002、CTX-003 均已完成（看板 22/28）。
- 已有能力：RunEventMsg 按 SessionID 路由（RUN-003）；job 重启归并 interrupted（JOB-004）；工作区事实模型（CTX-001）；file.edited 事件携带 Verify 命令（EDIT-003）；后台任务状态卡片（UI-001，无会话隔离字段）。
- 缺口：无切换时的状态重置；无重启后「哪些操作需重试」的汇总呈现；任务快照无会话隔离。

## 变更

- `internal/tui/resume.go`（新增）：`ResumePanel`——任务列表/最近修改/验证摘要三类事实，`[需重试]` 文字标记 interrupted/failed 任务，任意按键收起；渲染行宽受控。
- `internal/tui/app.go`：
  - `SwitchSession(id)`：重绑会话、清空运行观察点与流式状态、重建任务卡片（复用运行时钩子）、收起恢复面板、系统通知；同 ID 幂等；
  - `SetResumeItems`：装入恢复事实，有需重试项时给出计数通知；
  - `JobStatusMsg` 增加 `SessionID`：旧会话的快照不进入新会话的任务卡片（与 RunEventMsg 同隔离语义）。
- `cmd/basework/resume.go`（新增）：`collectResumeItems`——任务历史（interrupted/failed → 需重试）、工作区事实（file_modified，最多 8 条）、验证摘要（最后一次 committed 的 Verify 命令，附「建议重跑」）；任一路失败不阻塞其余。
- `cmd/basework/tui.go`：App 构造后显式 `SwitchSession(rt.sessionID())`；快照投递带 SessionID（`snapshotMsg` 闭包）。
- `cmd/basework/agent.go`：`getSessionDir` 改为 var 形式（测试可注入隔离目录；生产不变）。
- `docs/TUI.md`（新增）：TUI 操作指南——全局按键、任务卡片、审批弹窗、恢复面板语义与无提示体验检查清单。

## 验证

- 任务卡针对性验证：`go test -race -tags "sqlite memory" ./internal/tui ./cmd/basework -count=1` → **两包全 ok**。
- 验收条件逐条：
  - **切换后不串消息/成本/任务状态**：`TestApp_SwitchSession_Isolation`（旧会话 RunEventMsg/JobStatusMsg 均忽略，新会话事件接受）。
  - **重启后明确哪些操作需重试**：`TestCollectResumeItems`（interrupted → 需重试；succeeded 不标；文件/验证摘要齐全）；`TestResumePanel_RetryMarking`（文字标记与计数）。
  - **人工能完成修复→测试→退出→继续流程**：CLI 人工场景（隔离 HOME）——`jobs list` 显示 interrupted（重试依据）、`edits list --session` 显示冲突与验证命令（修复与复验依据）、`agent`/`tui --no-tui` REPL exit 退出码 0 无残留。真实 TUI 全屏击键留待 SHIP-001（无 TTY），键盘语义已由单元测试覆盖。
- 全量：双模式 34/34 包全绿（退出码 0）；`GOOS=windows`/`GOOS=linux` 交叉编译通过；`make check-arch`/`check-docs` 通过；`make gen` 幂等。
- 指南：`docs/TUI.md` 含「无提示体验检查清单」5 项，实机验收时逐项打勾。

## 剩余与交接

- 全屏 TUI 的真实渲染与按键手感需实机（SHIP-001）；本任务覆盖语义层与数据层。
- 运行成本展示（token 用量）不在本任务范围（任务卡未列，涉及 LLM 层计量，如需应另立任务卡）。

## 结论

UI-003 完成。会话切换正确解绑旧事件（运行事件与任务快照按会话隔离），重启后恢复面板明确标注哪些操作需重试/复核（interrupted 任务、失败命令、待复验的修改与验证命令），CLI 人工场景验证了修复→测试→退出→继续的完整依据链；TUI 操作指南落地 `docs/TUI.md`。

## 2026-09-15 修复复核

补齐 A09/A10/A12 的语义层：切换会话会取消当前轮、清理流式状态并递增 generation；
TUI 流式/思考/工具/错误回调带运行与会话标识，旧消息被过滤；Ctrl+C/Ctrl+D 在
流式期间只取消本轮，取消后仍可继续输入。新增
`TestAppCancelStreamingAndIgnoreStaleResponse`，并通过 `go test -race ./internal/tui`
与全量带 tag race。

当前没有全屏 TTY 下的真实会话选择与重启操作记录，真实 Provider 也未在本轮使用；
因此看板仍保留 UI-003“进行中”，待 QA-001/SHIP-001 补齐产品验收。

## 2026-09-15 真实切换入口复核

- 新增 TUI 用户命令 `/session <id>`。命令由 `internal/tui` 发起，`cmd/basework`
  的 runtime controller 负责先构造目标会话，再替换 Service、任务 owner 和事件订阅；
  新 runtime 构造失败时当前会话保持不变。
- 切换成功后，App 清理旧消息、取消当前轮、重置流式与任务卡片，并从目标会话事件
  投影历史消息；旧服务关闭后才继续新会话，迟到事件按 session/run 标识过滤。
- `SHIP003_ROOT=/private/tmp/ship003-s6-final2 python3 tests/tui_pty/scenario6.py`：通过。
  该场景先建立两个独立会话，在第二个会话中执行 `/session <first-id>`，断言第一会话
  的唯一用户事实恢复、第二会话事实不出现、服务退出无残留；同时用不存在的 ID 做负向
  验证，确认明确报错且不创建第三个会话。

本记录仍不把 UI-003 改为完成：CTX-002 的跨平台前置与候选发布验收尚未全部满足；
本段证明的是用户入口和会话隔离已经有真实 PTY 证据。

## 2026-09-15 恢复面板真实入口复核

`SHIP003_ROOT=/private/tmp/ship003-s3-ui3-final2 python3 tests/tui_pty/scenario3.py` 全部通过：
真实 TUI 在硬杀后按同一 session ID 重启，首屏显示遗留操作与“重试/复核”提示；CLI
历史确认 job 为 `interrupted`，模型请求数没有因重启增加，继续输入后假 Provider 确认
请求实际带有历史事实。该证据补齐了恢复面板的产品入口链路；迟到事件隔离仍由
`TestApp_Update_LateEventsIgnoredAfterSwitch` 等 App 级测试覆盖。

## 2026-09-15 任务收口

CTX-003 已完成前置依赖收口，且候选提交 `1e9c8198f6d29530692ed6d3a0dc3e1bbb1eb3b8` 的
[CI run 34945363069](https://github.com/wly2lcl/basework/actions/runs/34945363069) 中
Windows/macOS/Ubuntu 全量测试与 Quality race 门禁均通过。结合本记录的真实 PTY 场景
6（会话切换）和场景 3（恢复面板）以及 App 级迟到事件测试，UI-003 的任务验收已满足。

**当前结论：UI-003 完成。** 用户已指定以终端级自动化替代人工按键签收，因此真人主观
体验继续作为明确边界记录，不伪称为已验证。

## 2026-09-16 再复审交接

前置任务回退；本任务既有实现和局部证据保留，等待依赖修复后回验，不要求重写模块。 本次状态调整为待验证，小步骤见原任务卡新增补充。历史成功测试不删除，但不能代替本次缺陷修复后的验证。

## 2026-09-16 修复候选回验

候选 `3ad67a7d30ab5b030bd28fe09504b6134c5a6faf` 的 PTY scenario3、scenario6、scenario8
通过：硬杀后按同一 session ID 恢复、未知 ID 明确拒绝且不创建会话、会话历史不串入，
双压缩后继续请求能看到历史标记，遗留任务为 `interrupted` 且不自动重跑。UI-003 依赖
回验完成；真人主观手感仍是独立边界。
