# QA-001 验证记录：可复跑的产品验收与证据门禁
> **2026-09-16 再复审：下文完成结论均为历史记录。** B02/B03 已确认验收假阳性与秘密输出路径；B04/B06 的超时和 CI 缺口也需补齐。 当前状态仅见 [TASKS](../../TASKS.md)，依据见 [本次复审](REVIEW-2026-09-16.md)。

## 基线

- 历史候选基线：`HEAD=7a874c9`，当时工作区含未提交代码与文档修改；该段只用于追溯，不能冒充已提交候选版本。
- 核心业务代码候选：`9d7458bb5c53274b5d5b1742cb88abe75ad4ac64`；发布辅助脚本候选：
  `ef3041bc319690cce8e30aa9edd45af7a4a8c808`；当前主工作区在这些候选之后只增加了文档证据提交。
- 夹具入口：`tests/tui_pty/harness.py`。
- 关键修复：fixtures 固定从仓库读取；运行根默认由 `tempfile.mkdtemp` 创建；Go SDK 从 `SHIP003_GO_ROOT` 或 `go env GOROOT` 发现；二进制由当前 checkout 自动构建；fake provider 使用运行根传入的日志/PID 路径；场景 3 显式复用第一段 session ID 并校验历史唯一事实；场景 5 从仓库外临时 Go module 编译。
- 真实 Provider：本轮没有注入外部密钥，以下是脚本化 Provider 的产品入口证据；真实模型证据仍单独保留为发布验收缺口。

## 验证

以下命令均从仓库根执行，真实二进制、真实 PTY、本地回环 HTTP 假服务和隔离 HOME 均由夹具创建：

| 场景 | 结果 | 关键独立断言 |
|---|---|---|
| `SHIP003_ROOT=/private/tmp/ship003-s1-final3 python3 tests/tui_pty/scenario1.py` | 通过 | 读文件 → 预览 → 提交 → 测试 → 答复顺序；同一 session 重启绑定；磁盘修改；`facts show` 读到 `calc.go` 的 `file_modified`；23 个工具定义；5 轮流式请求；退出无残留 |
| `SHIP003_ROOT=/private/tmp/ship003-s2 python3 tests/tui_pty/scenario2.py` | 通过 | 后台 shell 与 go test PID 均可见；TUI 取消后均退出；`jobs list` 为 canceled 且无 running |
| `SHIP003_ROOT=/private/tmp/ship003-s3-ui3-final2 python3 tests/tui_pty/scenario3.py` | 通过 | 重启绑定同一 session ID；恢复面板展示遗留操作与重试/复核提示；历史含 `FACT-S3-ORCHARD`；假服务实际看到该事实；遗留 job 为 interrupted；请求数不增加 |
| `SHIP003_ROOT=/private/tmp/ship003-s4 python3 tests/tui_pty/scenario4.py` | 通过 | gpt-4o/gpt-3.5 能力提示、缺 key 错误和工具定义保持可见 |
| `SHIP003_ROOT=/private/tmp/ship003-s5-final python3 tests/tui_pty/scenario5.py` | 通过 | 仓库外 module 只导入 `pkg/*` 并构建运行；3 次工具调用；回调事件；25 条会话事件落盘 |
| `SHIP003_ROOT=/private/tmp/ship003-s6-final2 python3 tests/tui_pty/scenario6.py` | 通过 | TUI 内 `/session <id>` 真实切换；历史唯一标记恢复；第二会话消息不串入；退出无残留 |
| `SHIP003_ROOT=/private/tmp/ship003-s7-final5 python3 tests/tui_pty/scenario7.py` | 通过 | 真实审批弹窗展示工具/路径；拒绝不改盘；允许完成 edit_files；允许场景退出无残留 |
| `SHIP003_ROOT=/private/tmp/ship003-s8-final3 python3 tests/tui_pty/scenario8.py` | 通过 | 真实配置触发两次压缩；关闭后同一 session 重启；JSONL 有 2 个 compacted 事件；恢复请求看到第三轮历史；两段退出无残留 |

场景 5 输出示例：`嵌入验收通过: 会话=b0246513 工具调用=3 事件={text.delta:3 tool.started:3 tool.finished:3 turn.finished:1} 事件落盘=25 条`。

## 变更

- `.github/workflows/build.yml` 的 race 范围加入 `internal/runtime`、`internal/jobs`、`internal/edits`。
- CI quality job 增加 Unix PTY scenario1 烟测；完整 scenario2–5 仍按成本在本地/发布验收分组执行。
- `examples/embed/main.go` 不再导入 `internal/runtime`，改用 `pkg/agent`、`pkg/provider`、`pkg/session`、`pkg/tool` 公共接口与回调。
- 场景失败会回收假服务和 PTY；默认临时运行根在进程退出时清理，显式 `SHIP003_ROOT` 便于复盘时保留报告。

## 剩余与交接

- 本记录证明脚本化模型的真实入口闭环，不证明任意外部模型、真人体验或目标平台安装。
- 故意把历史唯一事实、session ID、请求日志、磁盘结果和进程状态分开断言；通用的“已切换到会话”提示不再构成恢复成功。

## 2026-09-15 当前工作区全量复跑

在同一个当前工作区、每个场景独立的 `SHIP003_ROOT`、隔离 HOME 和本地确定性
Provider 下，按 `scenario1.py` 到 `scenario8.py` 的顺序重新运行全量 PTY 集：

```text
for i in 1 2 3 4 5 6 7 8; do
  SHIP003_ROOT="/private/tmp/ship003-final-s$i" python3 "tests/tui_pty/scenario$i.py"
done
```

八个场景均退出 0，且逐场景报告“通过”。这次复跑新增确认：

现在同样可以使用仓库内的一条命令完成隔离编排：

```text
python3 tests/tui_pty/run_all.py
```

该入口为每个场景创建独立子进程和临时根，默认结束时清理；`--keep` 或 `--root`
用于保留报告。场景失败会停止后续场景并返回非零退出码，不依赖 shell 的循环语义。

本轮实际运行：

```text
python3 tests/tui_pty/run_all.py
```

结果为 scenario1–8 全部 `PASS (exit 0)`；默认临时父目录在进程结束后已清理。另用
`python3 tests/tui_pty/run_all.py --scenarios 3,8` 验证了筛选入口，两场均通过。

真实模型夹具现已随仓库保存于 [tests/real_provider/](../../../tests/real_provider/)；
使用说明见[真实 Provider 验收夹具](../real-provider-acceptance.md)：固定 `fixbug` 输入、独立
临时工作目录、夹具侧 `go test ./...`、工具轨迹摘要和脱敏 JSON schema。缺少
`BASEWORK_REAL_API_KEY`、`BASEWORK_REAL_BASE_URL` 或 `BASEWORK_REAL_MODEL` 时，
`go run ./tests/real_provider` 明确退出非零并输出“no request was sent”；本轮没有
伪造真实 Provider 结果，也没有把密钥写入文件。

runner 自身的脱敏与 fixture 复制单测 `go test ./tests/real_provider -count=1` 通过，
Windows 目标 `go test -c ./tests/real_provider` 也可交叉编译；这两项只证明夹具可构建，
不替代真实 Provider 请求结果。

`.github/workflows/real-provider.yml` 已提供手动、可追溯的真实 Provider 入口：输入
Provider/base URL/model，密钥只从 `BASEWORK_REAL_API_KEY` secret 注入，失败也上传
脱敏结果。当前没有配置 secret，也没有触发该 workflow，因此真实 Provider 证据仍待
补跑。

- scenario3 的恢复面板实际显示遗留操作和“重试/复核”，硬杀后没有自动重跑，
  同一 session 继续对话并读到磁盘历史事实；
- scenario6 的 `/session` 切换恢复第一会话历史，第二会话消息没有串入；
- scenario6 还覆盖未知 session ID 的负向门禁：明确报错、当前会话和会话文件数量不变，
  不会静默创建新会话；
- `PYTHONDONTWRITEBYTECODE=1 python3 tests/tui_pty/mutation_check.py` 在临时副本移除
  `opts.SessionID = sessionID` 接线后，scenario6 以非零退出并报告恢复历史断言失败；
  证明恢复测试不会因宽松提示而在关键接线被移除时继续通过。
- scenario7 的拒绝审批不改盘，允许审批后完成真实 `edit_files`；
- scenario8 的配置预算真实触发两次压缩，关闭后按同一 session 重启，JSONL 中有
  两个 `compacted` 事件，恢复请求实际看到 C8-3 历史标记；
- scenario1 在工具阶段实际看到状态栏“忙”，结束后恢复“空闲”，覆盖状态栏接线；
- 每个场景结束均无 PTY、假服务或测试子进程残留。

这组结果补齐了当前工作区的脚本化产品入口复跑证据，但仍不是候选 commit、目标
Windows/macOS/Linux 机器安装、真实 CI run 或外部 Provider 证据。QA-001 因此继续
保持“待验证”，直到这些外部门禁按任务卡要求有可追溯结果。

路径保护补丁后又单独复跑了恢复与压缩两个受影响场景：

```text
SHIP003_ROOT=/private/tmp/ship003-path-s3 python3 tests/tui_pty/scenario3.py
SHIP003_ROOT=/private/tmp/ship003-path-s8 python3 tests/tui_pty/scenario8.py
```

两条命令均退出 0；恢复面板、历史事实、不中断重跑、两次 `compacted` 事件和同一
session 继续请求的断言均通过。

在加入未知会话与 mutation 负向门禁后再次运行 `PYTHONDONTWRITEBYTECODE=1
python3 tests/tui_pty/run_all.py`，scenario1–8 仍全部 `PASS (exit 0)`；scenario6
额外验证未知 ID 报错且不创建新会话。

同日又在当前工作区完成最终本地回归：

```text
go test -count=1 ./...
go test -tags 'sqlite memory' -count=1 ./...
go test -race -tags 'sqlite memory' -count=1 ./internal/tui ./pkg/agent ./cmd/basework ./tests/real_provider
```

三条命令均通过；前两条覆盖默认与完整标签构建的全仓测试，第三条覆盖本轮状态栏、
事实路径、CLI 运行时和真实 Provider runner 的并发回归。`python3 tests/tui_pty/run_all.py`
在允许回环假服务绑定的本机权限下再次完成 scenario1–8，八个场景均退出 0；scenario1
额外确认运行中的状态栏显示忙碌状态，结束后恢复空闲。

随后执行 `GOCACHE=/private/tmp/basework-full-race-cache go test -race -tags 'sqlite memory' -count=1 ./...`，
全仓 34 个包（含 `tests/real_provider`）均通过。

本轮在可绑定回环端口的执行环境再次复核同一范围：

```text
GOCACHE=/private/tmp/basework-current-default-cache go test -count=1 ./...
GOCACHE=/private/tmp/basework-current-race-cache-escalated go test -race -tags 'sqlite memory' -count=1 ./...
```

两条命令均通过；默认构建和完整 tag race 各覆盖全仓 34 个包。先前无权限沙箱中的
回环监听失败已确认是执行环境限制，不计入产品失败。

随后在同一可绑定回环端口的环境执行：

```text
PYTHONDONTWRITEBYTECODE=1 python3 tests/tui_pty/run_all.py
```

scenario1–8 全部 `PASS (exit 0)`；scenario1 验证忙碌状态栏，scenario3/6/7/8
分别验证恢复、未知会话拒绝与切换、审批拒绝/允许、双压缩重启。

## 2026-09-15 干净提交 scenario7 竞态修复

在 `b885f14` 的干净 worktree 首次全量 PTY 复核中，scenario1–6、8 通过，scenario7
唯一失败为“权限确认”标题出现后立即读取屏幕，详情行尚未完成下一次渲染；允许/拒绝
业务本身和文件保护断言均通过。该失败属于验收驱动的屏幕读取竞态，不把它记成产品
通过。

已将 `tests/tui_pty/scenario7.py` 改为分别等待 `工具:` 和 `路径:` 详情出现后再断言，
并在当前提交后连续运行 5 次独立 scenario7：5/5 `PASS (exit 0)`。因此后续全量
候选验收必须使用包含该修复的提交，不能复用 `b885f14` 的一次性全量结果。

随后将该修复固定为提交
`9d7458bb5c53274b5d5b1742cb88abe75ad4ac64`，在全新 detached worktree 中复核：
`git status --short` 为空，`make check-docs` 通过，
`go test -race -tags 'sqlite memory' -count=1 ./...` 全仓通过，且
`PYTHONDONTWRITEBYTECODE=1 python3 tests/tui_pty/run_all.py` 的 scenario1–8 全部
`PASS (exit 0)`。这是当前本地提交级证据；远端 CI 仍需该提交进入验证分支后才能闭环。

## 2026-09-15 清洁候选 checkout 复核

为验证“全新 checkout + 全新测试目录”，将当前完整工作树复制到独立临时仓库并创建
一次性候选提交：

```text
candidate commit: ac6852dc33d77a01ccb927e5a6cc46dafad804d5
git status --short: clean
make gen && make check-docs
git diff --exit-code -- docs/DEPGRAPH.md docs/STATS.md
go test -tags 'sqlite memory' -count=1 ./...
PYTHONDONTWRITEBYTECODE=1 python3 tests/tui_pty/run_all.py
```

生成物新鲜度、文档门禁、全量 tag 测试和 scenario1–8 均通过。该 commit 只存在于
`/private/tmp` 的一次性 checkout，未推送到 origin，也没有冒充正式候选 CI；它证明
仓库内容可以脱离当前主工作区独立准备和运行，正式发布证据仍需绑定用户授权的候选提交。

## 结论

夹具、外部 module 示例和 Unix PTY 场景已经具备可复跑证据；当前工作区的
scenario1–8 全量入口也已通过。任务状态仍保持“待验证”，因为任务卡还要求真实
模型与脚本模型分离、候选 commit/CI 结果可追溯，以及发布平台验收。

## 2026-09-15 候选 CI 证据补齐

候选提交 `1e9c8198f6d29530692ed6d3a0dc3e1bbb1eb3b8` 的 [GitHub Actions run
34945363069](https://github.com/wly2lcl/basework/actions/runs/34945363069) 已全绿。Quality
job 的 race、文档、架构与 Unix PTY smoke，三平台全量测试，Docker Smoke 和 Release Dry
Run 均有独立 job 结果；脚本化 Provider 场景与外部 Provider 仍在本记录中分开描述。

这次运行满足 QA-001 验收清单中“CI job 的具体范围、commit 和结果可查”一项，但没有
注入 `BASEWORK_REAL_API_KEY`，也没有把 CI 源码构建误写成发布归档安装。因此：

**当前结论：QA-001 仍待验证。** 还需要一次当前候选的真实 Provider 运行，并在发布
任务中完成目标平台归档安装/升级证据；缺少凭据时不得伪造正向结果。

## 2026-09-15 最新候选回填

候选提交 `6a2cc3e72aaa02ab87b72067545c42cec92fbd17` 的 [GitHub Actions run
34946819197](https://github.com/wly2lcl/basework/actions/runs/34946819197) 在新增
`Release Artifact Smoke` 后仍全绿：三平台归档分别解包运行，Docker Smoke、Release Dry
Run、Quality 与三平台全量测试均通过。该结果补齐了 QA-001 的候选 CI 可追溯门禁，但仍
没有 `BASEWORK_REAL_API_KEY`，也没有把脚本化 Provider 当成真实 Provider。

**当前结论：QA-001 仍待验证。** 剩余是当前候选真实 Provider 运行，以及发布矩阵中
Linux arm64/Darwin amd64 归档安装或明确缩小承诺范围。

## 2026-09-16 五平台候选门禁回填

候选提交 `341679791936b9af3657a711b27fa70413498895` 的 [GitHub Actions run
35041364486](https://github.com/wly2lcl/basework/actions/runs/35041364486) 全部通过。
Quality、五平台源码测试、五平台 Release Artifact Smoke、Docker Smoke 与 Release Dry Run
均有独立 job 结果；归档 runner 与 SHA-256 详见 [SHIP-002](SHIP-002.md#2026-09-16-完整五平台归档-smoke-与哈希)。

这次回填关闭了 QA-001 的候选 CI、Docker 和五平台归档证据缺口。当前仍没有
`BASEWORK_REAL_API_KEY`，因此未发出真实 Provider 请求，也没有把脚本化 Provider 结果当成
真实模型结果。

**当前结论：QA-001 仍待验证。** 唯一剩余的自动化外部门禁是当前候选真实 Provider 正向
运行；缺少凭据时保持待验证，不伪造通过结果。

## 2026-09-16 当前候选发布包与迁移门禁回填

候选提交 `3787862cd076b72d49acfc3c1127efcc807f3539` 的 [GitHub Actions run
35043445341](https://github.com/wly2lcl/basework/actions/runs/35043445341) 全绿。Quality、
五平台源码测试、五平台发布归档 Smoke、Docker Smoke 和 Release Dry Run 均有独立 job 结果。
本次发布归档 Smoke 还在每个平台解包运行 `init`，执行 v1 JSONL → SQLite 迁移并检查源文件
哈希未变、`upgrade-v1` 可查询，以及 v99 未来格式被拒绝且未导入；具体归档与 SHA-256 见
[SHIP-002](SHIP-002.md#2026-09-16-当前候选发布包迁移-smoke)。

本次 run 未注入 `BASEWORK_REAL_API_KEY`，没有发出真实 Provider 请求，也没有把脚本化 Provider
结果写成真实模型结果。因此 QA-001 仍待验证，剩余一项为当前候选真实 Provider 正向运行。

## 2026-09-16 当前候选真实 Provider 正向与验收收口

当前候选提交 `2a868864f31c8a66527c1679fd2951a8869c2267` 的 [真实 Provider workflow
35049625067](https://github.com/wly2lcl/basework/actions/runs/35049625067) 成功完成。
工作流明确使用 `openai`、`https://newapi.doubb.top/v1`、`agnes-2.5-flash`，只从
`BASEWORK_REAL_API_KEY` secret 注入密钥，并上传脱敏结果
[`SHIP-001-real-provider-2026-09-16.json`](SHIP-001-real-provider-2026-09-16.json)。

结果证明：真实 Agent 回合成功、固定实现文件发生修改、独立 `go test ./...` 退出码为 0，
工具名与成功状态可追溯，且没有保存完整模型输出或密钥。该文件与仓库内的脚本化 PTY/离线
证据分开，满足 QA-001 的“真实模型与脚本化模型证据分开”要求；候选 commit、workflow、job
和结果 artifact 均可查。

这次正向运行关闭了 QA-001 唯一剩余的自动化外部门禁。真人主观 TUI 手感仍按任务卡单独
标为体验边界，不冒充自动化结果，也不阻塞本任务的可复跑验收。

**当前结论：QA-001 完成。**

## 2026-09-16 再复审交接

B02/B03 已确认验收假阳性与秘密输出路径；B04/B06 的超时和 CI 缺口也需补齐。 本次状态调整为进行中，小步骤见原任务卡新增补充。历史成功测试不删除，但不能代替本次缺陷修复后的验证。

## 2026-09-16 修复进展

- B02：真实 Provider runner 建立可信夹具副本，只把未受保护的实现 `.go` 文件合并到验证目录；测试文件、`go.mod` 与其他夹具文件发生变化或新增文件都会拒绝。验证固定执行 `go test -count=1 -run ^TestAdd$ ./...`，结果含 `[no tests to run]` 或未实际执行预期用例时失败。
- B03：`builtin.Runtime.Environment` 支持为 Bash 子进程注入显式环境；runner 过滤已知 Provider key，recorder/结果错误字段也做密钥替换。新增合成密钥隔离回归，不触碰真实凭证。
- B04：独立测试使用 60 秒 context、有界 64 KiB 输出和 Unix 进程组/Windows taskkill 进程树取消；超时回写结构化失败。新增短超时与派生子进程测试。
- B06：Quality job 新增无 tags 的 `go test -count=1 ./...`，保留完整 `sqlite memory` tags 测试和现有 race/平台门禁。

定向测试：`go test -tags "sqlite memory" ./tests/real_provider ./internal/runtime ./pkg/tool/builtin -count=1`、默认 `go test -count=1 ./...`、完整 `go test -tags "sqlite memory" ./...` 均通过；文档中的 B01–B03 修复回归命令通过。B05 的当前候选协议次数/入口矩阵和完整产品依赖验收仍未完成，因此本任务继续进行中。

## 2026-09-16 修复候选本地与 CI 回验

候选 `3ad67a7d30ab5b030bd28fe09504b6134c5a6faf` 已完成本地提权环境的默认/完整 tags
全量测试、相关 race、`go vet`、架构检查、`make gen`/`make check-docs progress` 和
PTY scenario1–8。合成 Provider key 不出现在工具结果或 JSON；可信测试篡改、正确实现、
超时/派生子进程和有界输出回归均通过。

同一候选的 [CI run 35055131884](https://github.com/wly2lcl/basework/actions/runs/35055131884)
最终全绿，Quality job 已实际运行无 tags 的 `go test -count=1 ./...`，并保留完整 tags、
相关 race、五平台源码/归档、Docker Smoke 与 Release Dry Run。首次 macOS runner 的
`pkg/lsp/TestClientConcurrentStart` 10 分钟超时已通过重跑恢复，失败日志保留在 SHIP-002。

当前 QA-001 仍保持进行中：第 1、2 次 OpenAI-compatible 真实 Provider 运行均以端点停滞
失败（分别约 451 秒、187 秒），第 3 次通过，第 4 次以端点 `504` 失败；其中第 1 次脱敏结果见
[`SHIP-001-real-provider-2026-09-16-run-35055166589.json`](SHIP-001-real-provider-2026-09-16-run-35055166589.json)。
第 3 次结果见 [35070656461](https://github.com/wly2lcl/basework/actions/runs/35070656461)，
第 4 次结果见 [35070828659](https://github.com/wly2lcl/basework/actions/runs/35070828659)。在真实
Provider 协议/次数矩阵完成前，不把本地假 Provider 或旧候选结果写成当前通过。

随后第 5 次 [35071026911](https://github.com/wly2lcl/basework/actions/runs/35071026911)、第 6 次
[35071530401](https://github.com/wly2lcl/basework/actions/runs/35071530401) 和第 7 次
[35071644174](https://github.com/wly2lcl/basework/actions/runs/35071644174) 均通过；三次均有
`tests_executed=true`、独立测试退出码 0、测试/实现哈希和脱敏 JSON。OpenAI-compatible
入口因此满足连续 3 次要求；QA-001 仍等待不同协议 Provider 的当前候选证据，且不把历史
多协议记录冒充本轮结果。

OPT-003 随后提交为 `8394227`，因此上述真实 Provider 三次通过仍只证明上一候选
`3ad67a7`；当前候选需推送后重新生成远端 CI/真实 Provider 证据。优化本身的本地全量、
race、PTY 和性能验证见 [OPT-003](OPT-003.md)。

## 2026-09-16 当前远端候选 CI 验证

提交 `4c40e18e2662166902500bf1ba18124efdfc5a9f` 的 [GitHub Actions run
35077896624](https://github.com/wly2lcl/basework/actions/runs/35077896624) 已全绿：Quality、五平台源码测试、五平台发布归档 Smoke、Docker Smoke 与 Release Dry Run 均通过。
Quality 还完成了默认测试、vet、架构/文档门禁、race、Unix PTY smoke 和 OPT-001 smoke；生成统计刷新已在该提交中固定。

这条记录只证明当前远端候选的 CI 与发布辅助门禁，不能延伸上一候选的真实 Provider 结果。
当前候选的 OpenAI-compatible 连续 3 次真实通过，以及不同协议的连续 3 次真实通过，仍需按
SHIP-001 的原任务卡重新生成并绑定实际 workflow run。

## 2026-09-17 当前远端文档候选 CI 验证

提交 `140eddf4cfd0ba457d7b82623c5e83d1b3c46574` 的 [GitHub Actions run
35079524005](https://github.com/wly2lcl/basework/actions/runs/35079524005) 已完成且全绿。Quality、五个平台源码测试、五个平台发布归档 Smoke、Docker Smoke 与 Release Dry Run 均成功，Quality 还实际运行了默认无 tags 全量测试、race、Unix PTY smoke 和 OPT-001 smoke。

该结果补齐当前远端候选的 CI 可追溯证据，但不包含真实 Provider 请求。QA-001 仍需当前候选的真实 Provider 正向结果；脚本化 Provider、历史候选和本次 CI 不得互相替代。

## 2026-09-17 CI 竞态修复与本地回归

文档候选 `612f670` 的 [CI run 35168351723](https://github.com/wly2lcl/basework/actions/runs/35168351723) 在 macOS Intel 的 `internal/jobs` 全量测试中失败，日志为 `TestTerminateCommand_KillsGrandchildren` 读取到空 pid 文件。该失败不是业务逻辑结果：shell 重定向会先创建空文件，测试的 `os.Stat` 等待条件过早返回。

同次本地 sqlite/memory 全量还在 `pkg/lsp.TestClientConcurrentStart` 卡住；goroutine 栈显示并发握手因共享 `c.conn` 被覆盖而互相等待。提交 `66b7cc041d8b0f49667af4ca2eeb7cd04e203825` 修复了两处竞态。验证结果：进程树测试重复 100 次通过，LSP 并发测试重复 50 次及 race 重复 20 次通过，jobs/LSP 联合回归、默认 `go test -count=1 ./...` 和 sqlite/memory `go test -tags 'sqlite memory' -count=1 -timeout=5m ./...` 均通过。

该段只记录当时的修复候选；当前候选的 CI 结果见下方 2026-09-17 证据矩阵，不能把旧段的
“待回验”状态读成当前结论。

## 2026-09-17 当前候选证据矩阵

当前代码候选为 `dd08592adce712af73f1b235dfc829352dc31f1d`。OpenAI-compatible 真实 Provider
连续通过序列为 runs `35170019011`、`35170100696`、`35170184392`；三次均满足
`agent_ok=true`、`validation_ok=true`、`tests_executed=true`、`file_changed=true`，独立测试
退出码为 0，并保存测试/实现 SHA-256。前两次 `35169409650`、`35169557341` 也通过；第 3 次
`35169796044` 被可信门禁拒绝（`validation_ok=false`、`file_changed=false`），已保留为失败
样本，不跨失败计入连续序列。全部结果文件见 [SHIP-001](SHIP-001.md)。

| 验收维度 | 当前结果 | 证据/边界 |
|---|---|---|
| 凭证隔离与脱敏 | 通过 | 6 份 artifact 未出现 API key；密钥只由 Actions secret 注入 |
| 可信独立测试 | 通过 | 正向样本含原始测试/实现哈希与实际执行标记；失败样本不会被接受 |
| 超时、输出与进程清理 | 通过 | 本地 B04 回归与当前正向样本均有确定退出码；实时失败样本仍保留 |
| 默认/完整 tags CI | 通过 | 推送证据提交后的 run `35170954145` attempt 2 全绿；attempt 1 的 macOS Intel 网络型失败已重跑 |
| OpenAI-compatible 当前候选 | 通过 | 连续 3 次 runs `35170019011`/`35170100696`/`35170184392` |
| 不同协议当前候选 | 未完成 | 需要用户明确协议、端点、模型和授权；历史 REL-003 不可替代 |

当前 QA-001 仍为进行中：代码修复、OpenAI-compatible 子门禁和候选 CI 已完成，但不同协议矩阵
尚未完成。后续证据提交只包含文档、脱敏 artifact 和生成物刷新；真实 Provider 结果
继续绑定 `dd08592` 代码候选，不将证据提交后的新文档 SHA 误写成执行代码版本。

推送证据提交 `ea8a53c` 的 CI run `35170954145` 首次 attempt 1 仅在 macOS Intel 的 GoReleaser
依赖下载阶段因 `proxy.golang.org` DNS 超时失败；attempt 2 只重跑失败 job 后全绿，Quality、五
平台测试/归档、Docker Smoke 和 Release Dry Run 均通过。该网络失败不改变代码结论，但已作为
可追溯的失败样本保留。

## 2026-09-17 SEC-001 前候选真实 Provider 回验

最新远端可执行候选为 `66c57fde7f88ccaf5de0669bbd08c488a5686f0e`（代码行为包含
`150e77e`，其后仅有文档提交）。在同一 `main` 候选上连续触发 3 次
`openai` / `https://newapi.doubb.top/v1` / `agnes-2.5-flash`，workflow run 分别为
[35178297077](https://github.com/wly2lcl/basework/actions/runs/35178297077)、
[35178409921](https://github.com/wly2lcl/basework/actions/runs/35178409921) 和
[35178503811](https://github.com/wly2lcl/basework/actions/runs/35178503811)。三次结果均为
`agent_ok=true`、`validation_ok=true`、`tests_executed=true`、`file_changed=true`，独立
测试退出码均为 0；脱敏 JSON 已随 SHIP-001 证据保存。

这次回验关闭了当时候选的 OpenAI-compatible 正向证据缺口，入口范围为真实核心 Agent
API。随后 `be737c0` 新增 SEC-001 权限缓存/审计边界、旧规则兼容和 help，需重新绑定当前候选；不同协议 Provider、
真实 CLI/TUI 入口和真人 TUI 手感仍不由这三次结果覆盖，因此 QA-001/SHIP-001 的任务状态继续
按看板保留，不能提前标记整项完成。

## 2026-09-17 最终候选回验

最终远端候选为 `a78efba5eabbc9a7391986c86e8b234593bf862c`。`763a845` 推送后发现
`cmd/basework/permission.go` 帮助文本缩进未经过 `gofmt`，提交 `a78efba` 修复后重新跑完整
CI；[run 35188993611](https://github.com/wly2lcl/basework/actions/runs/35188993611) 的 Quality、
五个平台源码测试、五个平台发布归档 Smoke、Docker Smoke 和 Release Dry Run 全部通过。

同一最终候选使用 `provider=openai`、`https://newapi.doubb.top/v1`、`agnes-2.5-flash` 连续
运行 3 次：[35189820351](https://github.com/wly2lcl/basework/actions/runs/35189820351)、
[35189839503](https://github.com/wly2lcl/basework/actions/runs/35189839503)、
[35189879941](https://github.com/wly2lcl/basework/actions/runs/35189879941)。三次均满足
`agent_ok=true`、`validation_ok=true`、`tests_executed=true`、`file_changed=true`，独立
`go test -count=1 -run ^TestAdd$ ./...` 退出码为 0；三份脱敏结果已随 SHIP-001 保存，未发现
API key。该结果只关闭 OpenAI-compatible 当前候选子门禁，仍不覆盖不同协议、CLI/TUI 入口或
真人 TUI 手感。

**当前结论：QA-001 的 CI 与 OpenAI-compatible 当前候选子门禁已完成；任务继续进行中，等待
不同协议 Provider 连续 3 次当前候选证据。**

## 2026-09-17 Windows 超时夹具回归

文档说明提交 `fb55672` 的 [CI run 35191971964](https://github.com/wly2lcl/basework/actions/runs/35191971964)
在 `windows-latest` 的 `TestIndependentTestHonorsTimeoutAndBoundsOutput` 收尾阶段失败。日志显示
超时已触发且 `go test` 主进程退出，但其派生进程仍占用 fixture 目录，`testing.TempDir` 的
`RemoveAll` 因 Windows 文件句柄锁定失败；这不是业务断言失败，也不是网络错误。

修复提交将 `tests/real_provider/process_windows.go` 的取消路径改为 PowerShell 进程树快照、
逆序强制终止并等待所有已捕获 PID 消失，PowerShell 受限时回退 `taskkill /T /F`。本地
`go test -count=1 ./tests/real_provider` 和 `GOOS=windows GOARCH=amd64 go test -c`
均通过；任务仍需用修复提交的远端 CI 重新确认 Windows runner。
