# M2：后台命令与输出

状态只在 [任务看板](../TASKS.md) 更新。执行前读 [AI 开发流程](../development/ai-workflow.md)。下面是实施范围，新增名称均为拟定；发现已有实现先验证缺口。每张卡可分多个小提交，但验收全部满足后才算完成。

<a id="job-001"></a>

## JOB-001：定义并实现 job 状态与归属

**前置任务**：BASE-001。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`pkg/tool/builtin/bash.go`、`internal/tools/`、`pkg/session/event.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 核对同步 Bash 接口与权限入口。
2. 在拟新增 internal/jobs 定义状态及 owner/session/job ID。
3. 实现启动、状态查询与终态幂等。
4. 保持 pkg/tool 接口兼容，产品适配器返回结构化 job ID。

**验收条件**：

- 会话之间不能读/取消对方 job。
- 重复结束不会重复发终态。
- 并发数量与启动失败有明确返回。

**针对性验证**：新增 internal/jobs 后运行 go test -race ./internal/jobs ./internal/tools -count=1。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/JOB-001.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="job-002"></a>

## JOB-002：增量输出与文件溢出

**前置任务**：JOB-001。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/jobs/（JOB-001 新增）`、`internal/tools/`、`pkg/session/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 定义 stdout/stderr 偏移与分页读取。
2. 设定内存、单次返回、总存储配额。
3. 超量输出写私有文件并给可检索引用。
4. 实现输出保留期与关闭文件清理。

**验收条件**：

- 大输出不无限占内存。
- 分页不会漏/重复字节且 UTF-8 边界可处理。
- 越权读取被拒，磁盘满/输出过期有明确说明。

**针对性验证**：go test -race ./internal/jobs ./internal/tools -count=1；增加大输出和磁盘写失败场景。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/JOB-002.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="job-003"></a>

## JOB-003：超时取消与进程树清理

**前置任务**：JOB-002。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/jobs/（新增）`、`pkg/tool/builtin/timeout.go`、`cmd/basework/runtime.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 区分用户取消、deadline 和正常非零退出。
2. 实现平台适配的进程组终止与宽限期后强制结束。
3. 接入 owner 关闭清理。
4. 重放退出与取消同时发生等时序。

**验收条件**：

- 取消后子进程无残留。
- 终态只发布一次且原因正确。
- 不支持的平台行为显式说明并有对应 CI 验证。

**针对性验证**：go test -race ./internal/jobs -count=1；目标平台进程树实测并记录。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/JOB-003.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="job-004"></a>

## JOB-004：后台任务恢复与 CLI 接线

**前置任务**：JOB-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`cmd/basework/runtime.go`、`cmd/basework/session.go`、`pkg/session/`、`internal/tools/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 注册 job 查询/输出/取消工具并沿用权限。
2. 持久化命令摘要、状态、输出引用和退出结果。
3. 重启时把无法接管的旧运行任务标记 interrupted。
4. 在 CLI 回看结果并确认不会自动重跑命令。

**验收条件**：

- 长命令启动后 Agent 可继续处理。
- 恢复会话能看到历史结果。
- 重启不伪称进程仍运行，不自动重放副作用。

**针对性验证**：go test -tags "sqlite memory" ./cmd/basework ./pkg/session ./internal/tools -count=1；运行/取消/重启人工场景。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/JOB-004.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P1。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A06；A03 的产品入口由 CTX-003 收口）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 先复制复审探针 TestReviewJournalAppendAfterTruncatedTail；在 started + 半行残尾后追加 terminal，当前会损坏历史。
2. 在追加之前识别并安全处理仅最后一条不完整记录；保留可诊断的坏尾，不能吞中间损坏。读写/恢复使用一致锁策略，验证并发追加不会截断另一写者。
3. 保留显式 owner 的恢复接口；定义单进程占有或存活检测，不能把另一个活进程的任务标成 interrupted。选择旧会话的产品入口由后续 CTX-003 负责，避免反向依赖。
4. 覆盖正常尾、无换行完整 JSON、残缺尾、中间损坏、磁盘失败、重复恢复；用明确 owner 的 Manager 重建验证日志恢复；CLI/TUI 真实旧会话启动与两次压缩一起交由 CTX-003 验收。

**补充验收**：

- [ ] 残尾后可继续追加，旧完整记录与新记录均可读取；重复恢复不追加重复终态。
- [ ] 明确 owner 的崩溃遗留任务通过恢复接口变为 interrupted，仍在运行的其他 owner 不变；不自动重跑。产品入口的最终验收不能用此组件结果代替。

**针对性验证**：go test -race ./internal/jobs ./internal/tools ./cmd/basework -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。
