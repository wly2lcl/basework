# M3：可靠编辑与变更检查

状态只在 [任务看板](../TASKS.md) 更新。执行前读 [AI 开发流程](../development/ai-workflow.md)。下面是实施范围，新增名称均为拟定；发现已有实现先验证缺口。每张卡可分多个小提交，但验收全部满足后才算完成。

<a id="edit-001"></a>

## EDIT-001：统一编辑预览和冲突契约

**前置任务**：BASE-001。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`pkg/tool/builtin/edit.go`、`pkg/tool/builtin/write.go`、`internal/tools/applypatch.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 核对所有写入口与权限/路径检查。
2. 定义产品层 edit plan 含基线哈希、相对路径、diff 和 operation ID。
3. 实现只生成预览不写盘。
4. 覆盖符号链接、路径穿越和编码/换行。

**验收条件**：

- 预览不会改文件。
- 所有目标都经过权限与路径检查。
- 基线能用于提交前冲突验证，不能只依赖文件时间。

**针对性验证**：go test ./pkg/tool/builtin ./internal/tools -count=1。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/EDIT-001.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="edit-002"></a>

## EDIT-002：批量提交与安全撤销

**前置任务**：EDIT-001。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tools/`、`pkg/tool/builtin/`、`pkg/session/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 在批准后再次校验基线与权限。
2. 实现同文件多处与多文件计划的准备/写入流程。
3. 明确逐文件原子性与跨文件失败补偿，记录部分成功清单。
4. 撤销前确认文件仍等于本次写入结果。

**验收条件**：

- 冲突时不覆盖用户新改动。
- 中途失败有准确已写/未写列表。
- 撤销不覆盖后来编辑，不宣称跨文件全局原子事务。

**针对性验证**：go test ./pkg/tool/builtin ./internal/tools ./pkg/session -count=1；注入第 N 个文件写失败。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/EDIT-002.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P1。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A01）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 先加入复审中的两个 Rollback 失败场景：权限撤回，以及父目录替换为指向外部临时目录的软链。
2. 让撤销重新获得 Planner 的路径解析和 CheckPath 能力；逐文件校验工作区、真实路径、当前权限和结果哈希，拒绝时返回可见 outcome。
3. 保留逐文件原子替换与部分成功清单，不扩大为跨文件事务；处理撤销中途失败与仍可重试的状态。
4. 补父目录/文件软链、目标重定向、用户二次编辑、允许路径改变、重复撤销测试。

**补充验收**：

- [x] 权限撤回或目标逃逸后，工作区外及受保护文件逐字节不变。
- [x] 普通撤销仍恢复本次成功写入的内容；冲突/失败/跳过清单与磁盘一致。

**针对性验证**：go test -race ./internal/edits ./internal/tools -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。

<a id="edit-003"></a>

## EDIT-003：编辑事实与 CLI 审阅闭环

**前置任务**：EDIT-002。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`cmd/basework/runtime.go`、`internal/tools/`、`pkg/session/filetrack.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 将 edit plan/commit/result/undo 对应到可追溯事件。
2. 记录 touched files 与验证命令引用。
3. CLI 展示差异和结果并接既有批准策略。
4. 验证恢复会话可定位修改且不会重复提交。

**验收条件**：

- 用户可看改动前后和失败原因。
- 批准与拒绝都不会被绕过。
- 会话恢复后变更记录仍可用。

**针对性验证**：go test -tags "sqlite memory" ./cmd/basework ./internal/tools ./pkg/session -count=1；人工批准/拒绝/冲突场景。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/EDIT-003.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P1。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A01、A08、A11）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 沿用已存在的 edit_files 与事件格式，等 EDIT-002 修复后通过真实 runtime 工具调用验证撤销，而非只测 Planner。
2. 和 UI-002 对齐：用户审批基于服务保存的 plan_id 对应路径/基线/最终 diff；模型知道 plan_id 不等于用户批准。
3. 检查批量部分成功与撤销部分失败：逐文件 written/reverted 事实不能被整批 failed 掩盖；提交/撤销错误后保留可重试信息。
4. 给同 plan 的 commit/rollback 建立互斥状态转换，补并发双提交、提交与撤销交错、事件写入失败的可见性验证。

**补充验收**：

- [x] 交互拒绝后无写入；批准内容、落盘内容与事件中逐文件结果相符。
- [x] 重复与并发调用不重复副作用；CLI 审阅和事实聚合保留部分成功结果。

**针对性验证**：go test -race -tags "sqlite memory" ./internal/tools ./internal/edits ./cmd/basework -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。
