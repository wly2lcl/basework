# M8：评估与发布准备

状态只在 [任务看板](../TASKS.md) 更新。执行前读 [AI 开发流程](../development/ai-workflow.md)。下面是实施范围，新增名称均为拟定；发现已有实现先验证缺口。每张卡可分多个小提交，但验收全部满足后才算完成。

<a id="ship-001"></a>

## SHIP-001：固定编码场景回归集

**前置任务**：REL-003, CTX-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`tests/`、`docs/development/evidence/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 建立小函数修复、多文件修改、大输出、权限拒绝、断流恢复样例。
2. 固定输入/测试断言和产物而非模型文案。
3. 离线重放覆盖可确定逻辑，真实模型结果另存。
4. 记录成功率、耗时、调用量及失败类型。

**验收条件**：

- 每个场景可重复运行且清理独立。
- 不把一次成功当稳定成功率。
- 费用未知明确标未知，不能由 token 数编造账单。

**针对性验证**：go test -tags "sqlite memory" ./tests -count=1；真实场景按 validation.md 执行。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/SHIP-001.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P2。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A14）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 保留现有六个离线场景测试；它们已经通过，不需要重写模型文案断言。
2. 前置 CTX-003 修复后，在候选代码上重新运行涉及恢复/多文件编辑的完整产品流程；覆盖 partial commit、取消后继续和双压缩重启。
3. 与 QA-001 共用随仓库保留的无密钥真实模型夹具；保存脱敏输入、工具轨迹、独立测试结果、耗时与失败样本，不能只留 /private/tmp 脚本名。
4. 按 REL-003 的协议样本要求执行，分别注明核心 API/CLI/TUI 覆盖；未运行或凭据不足就保留验证缺口。

**补充验收**：

- [ ] 全新 checkout 能准备同样的场景；每个成功结果可由磁盘/进程/请求记录独立验证。
- [ ] 恢复相关依赖重新验收，历史模型数据不充当新候选版本的验证。

**针对性验证**：go test -tags "sqlite memory" ./tests -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。

<a id="ship-002"></a>

## SHIP-002：跨平台安装与升级验证

**前置任务**：SHIP-001, UI-003, CFG-002。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`docs/installation.md`、`docs/release.md`、`.goreleaser.yml`、`.github/workflows/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 在隔离目录或干净机器验证承诺的平台产物。
2. 验证初次配置、旧版本会话升级与未知高版本拒绝。
3. 执行打包 dry-run 且核对产物文档。
4. 记录系统、产物哈希与缺失平台。

**验收条件**：

- 不能用本机交叉编译冒充目标系统运行。
- 安装说明只包含已接入渠道。
- 升级不破坏原用户数据且有可执行恢复说明。

**针对性验证**：CI 平台测试/构建及 release dry-run；各承诺目标平台实际安装证据。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/SHIP-002.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P2。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A14）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 用实际候选 commit 构建发布包并记录 commit、build tags、产物 SHA-256；旧工作区快照包不能直接复用为新候选证据。
2. 在目标系统下载/解包同一产物运行 version/init/config explain，并验证 v1→当前迁移、未来版本拒绝、源数据保留。
3. 明确五个 OS/架构的产物矩阵；CI 跑源码测试/交叉编译不是安装验证。在 CI runner 或独立机器运行产物都有效，无需执着人工实机。
4. Docker 使用 buildx 构建 linux/amd64 与 linux/arm64 并运行版本/挂载工作区/只读命令冒烟，核对运行用户和目录权限；dry-run --skip=docker 不算镜像验证。
5. 任何拟承诺渠道缺少结果时继续留验证缺口，或经明确范围决定在发布声明中缩小支持范围。

**补充验收**：

- [ ] 每个声明已验证的平台有实际产物运行结果和哈希；CI 与候选代码的关系可追溯。
- [ ] 镜像构建与运行有记录；迁移失败不产生不可解释的半导入。

**针对性验证**：按 development/validation.md 的候选产物矩阵执行。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。

<a id="ship-003"></a>

## SHIP-003：候选版本验收与文档收口

**前置任务**：SHIP-002, QA-001。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`docs/STATUS.md`、`docs/CHANGELOG.md`、`docs/release.md`、`docs/development/evidence/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 在候选 commit 重跑发布检查。
2. 用真实 PTY 自动化完成路线图全部代表性场景，记录真人主观体验的独立边界。
3. 依据证据更新当前能力与限制，核对所有任务状态。
4. 产出候选版本验收报告，实际发布单独操作。

**验收条件**：

- 无未说明的 P0/P1 问题。
- 报告区分本地/CI/真实模型/人工体验。
- 准备完成不等于已经打 tag 或发布。

**针对性验证**：见 development/validation.md 的发布门槛；PTY 自动化验收记录（真人体验可另补）。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/SHIP-003.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P1。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A13、A14）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 等待已重开的前置项及 QA-001 满足后，选定一个候选 commit，记录文档、CI、安装产物和场景证据的对应关系。
2. 继续接受历史记录中由用户选择的真实 PTY 自动验收方式；不把真人主观体验当作本次唯一阻塞，也不将脚本化模型说成真人测试。
3. 重新运行五类代表场景并使用强断言：删掉恢复、摘要或压缩接线时，测试必须失败。
4. 核对 P1 问题已关闭、P2 问题有处理结果、平台/镜像矩阵齐全、文档命令可执行；发布授权仍单独处理。

**补充验收**：

- [ ] 报告无未解决的发布阻断项，证据不混用旧 dirty tree 和新候选 commit。
- [ ] 恢复/审批等真实产品流程完成；完成检查报告不能替代验收本身。

**针对性验证**：按 development/validation.md 执行候选验收。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。
