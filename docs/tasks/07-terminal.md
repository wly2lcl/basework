# M7：终端业务体验

状态只在 [任务看板](../TASKS.md) 更新。执行前读 [AI 开发流程](../development/ai-workflow.md)。下面是实施范围，新增名称均为拟定；发现已有实现先验证缺口。每张卡可分多个小提交，但验收全部满足后才算完成。

<a id="ui-001"></a>

## UI-001：工具与后台任务状态卡片

**前置任务**：RUN-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/`、`cmd/basework/tui.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 消费服务事件展示 queued/running/终态。
2. 按 job ID 读取分页输出。
3. 增加取消入口与快捷键说明。
4. 限制渲染量并验证小窗口。

**验收条件**：

- 工具开始/结束/失败/取消都可见。
- 大输出不卡死输入。
- 颜色之外有文字区分状态。

**针对性验证**：go test -race ./internal/tui ./cmd/basework -count=1；人工大输出/窄屏操作。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-001.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P2。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A10）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 先加入 60 行不足 4KB 的复现；分页按实际显示范围推进，保留未显示字节，上一页使用真实游标，不能用行数估算字节。
2. 统一 stdout/stderr 切换、UTF-8 边界、EOF、超长单行和过期输出提示；不要把“内存页已读完”当“屏幕全部展示完”。
3. 读取和取消从 Update 同步路径移到 tea.Cmd，避免等待文件 IO 或进程树终止阻塞按键；结果带 session/run/job 标识。
4. 修恢复面板宽度 11–23 的负切片，并覆盖 1/10/11/20/23/24/80 列；列表超过 50 项优先显示当前运行和最新任务。

**补充验收**：

- [x] 连续翻页能访问全部 60 行且不重复、不遗漏；反向翻页回到原页。
- [x] 小窗口不崩溃；任务取消/大输出时仍可处理键盘事件。

**针对性验证**：go test -race ./internal/tui ./cmd/basework -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。

<a id="ui-002"></a>

## UI-002：权限与编辑确认体验

**前置任务**：UI-001, EDIT-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/dialog/`、`internal/permission/`、`internal/tools/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 将批准请求作为带 ID 的 UI 事件。
2. 展示工具目的/路径/diff/风险依据。
3. 接入允许/拒绝/关闭窗口与超时。
4. 确认后提交仍检查基线和权限。

**验收条件**：

- 拒绝/关闭后不执行操作。
- 批准不会错误应用到下一请求。
- 对比结果与实际文件一致且键盘可完成操作。

**针对性验证**：go test -race ./internal/tui ./internal/permission ./cmd/basework -count=1；人工允许/拒绝/关闭弹窗。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-002.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P2。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A11）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 从产品中已保存的 edit plan 构造 commit/rollback 审批内容；不要仅从模型参数猜测路径和 diff。
2. 将请求 ID 与计划基线/内容摘要绑定，用户看到的必须对应将提交的文件；审批之后继续做权限和基线复查。
3. 补真实 edit_files 经权限 broker 到 App 的集成：允许、拒绝、关闭、超时、过期回答、错误计划、连续请求。
4. 审批为模态最高优先级；任务面板开着时也不能抢走审批的 enter/esc；关闭过期弹窗不能误关新弹窗。

**补充验收**：

- [x] commit 审批具有真实路径和 diff，空详情或失效计划不能被包装为已确认。
- [x] 所有拒绝路径不改盘；批准只适用于当前请求的计划，键盘操作可完成。

**针对性验证**：go test -race ./internal/permission ./internal/tui ./internal/tools ./cmd/basework -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。

<a id="ui-003"></a>

## UI-003：会话切换与可恢复进度

**前置任务**：UI-002, CTX-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/`、`cmd/basework/session.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 展示任务列表、最近修改和验证摘要。
2. 切换时正确解绑旧事件。
3. 区分失败、等待确认、用户取消与完成。
4. 更新 TUI 操作指南并执行无提示体验检查。

**验收条件**：

- 切换后不串消息/成本/任务状态。
- 重启后明确哪些操作需重试。
- 人工能完成修复→测试→退出→继续流程。

**针对性验证**：go test -race -tags "sqlite memory" ./internal/tui ./cmd/basework -count=1；人工端到端记录。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-003.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

### 2026-09-14 复审补充

**优先级**：P1。问题依据：[复审报告](../development/evidence/REVIEW-2026-09-14.md)（A03、A09、A10、A12）；先复跑相关 [最小复现](../development/review-reproduction.md)。原实现与历史测试保留，以下是继续工作清单。

**小步骤**：

1. 在 CTX-003 的旧会话恢复接口上实现产品可达的会话选择/切换操作；SwitchSession 方法本身不是用户入口。
2. 切换时取消/解绑旧运行，清理消息、流式状态、成本、输入状态、任务与审批；重绑读取/取消 owner，加载选中会话历史。
3. 投递旧的文本、工具结果、最终响应、错误、任务输出、审批和关闭消息，逐一证明不会进入新会话或误操作新任务。
4. 恢复面板从真实落盘事实与实际验证结果构建；verify 字段只表示声明的命令，未经执行不能称为“上次验证通过”。
5. 真实 PTY 验证修复→测试→退出→按同一 ID 继续；按 session ID 和历史唯一标记断言，不接受通用“切换到会话”提示作为恢复证据。

**补充验收**：

- [x] 选中的历史内容、成本和任务属于同一会话；旧任务迟到事件全部隔离。
- [x] 取消当前轮后可继续输入；退出后资源收回；恢复展示与持久化结果逐项一致。

**针对性验证**：go test -race -tags "sqlite memory" ./internal/tui ./cmd/basework -count=1。记录在本任务原证据文件的新日期段，保留旧记录；满足全部原有与补充验收后才更新看板。
