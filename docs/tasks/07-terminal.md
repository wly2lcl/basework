# M7：终端业务体验

状态只在 [任务看板](../TASKS.md) 更新。执行前读 [AI 开发流程](../development/ai-workflow.md)。下面是实施范围，新增名称均为拟定；发现已有实现先验证缺口。每张卡可分多个小提交，但验收全部满足后才算完成。

<a id="ui-001"></a>

## UI-001：工具与后台任务状态卡片

**前置任务**：RUN-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/`、`cmd/basework/tui.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 消费服务事件展示 queued/running/终态。
2. 按 job ID 读取分页输出。
3. 增加取消入口与快捷键说明。
4. 限制渲染量并验证小窗口。

**验收条件**：

- 工具开始/结束/失败/取消都可见。
- 大输出不卡死输入。
- 颜色之外有文字区分状态。

**针对性验证**：go test -race ./internal/tui ./cmd/basework -count=1；人工大输出/窄屏操作。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-001.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="ui-002"></a>

## UI-002：权限与编辑确认体验

**前置任务**：UI-001, EDIT-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/dialog/`、`internal/permission/`、`internal/tools/`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 将批准请求作为带 ID 的 UI 事件。
2. 展示工具目的/路径/diff/风险依据。
3. 接入允许/拒绝/关闭窗口与超时。
4. 确认后提交仍检查基线和权限。

**验收条件**：

- 拒绝/关闭后不执行操作。
- 批准不会错误应用到下一请求。
- 对比结果与实际文件一致且键盘可完成操作。

**针对性验证**：go test -race ./internal/tui ./internal/permission ./cmd/basework -count=1；人工允许/拒绝/关闭弹窗。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-002.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。

<a id="ui-003"></a>

## UI-003：会话切换与可恢复进度

**前置任务**：UI-002, CTX-003。依赖尚未完成时，只能调查或细化方案，不能宣称本项已验收。

**先读 / 主要修改范围**：`internal/tui/`、`cmd/basework/session.go`。首次阅读应确认实际文件/符号，拟新增路径不存在属正常。

**实施步骤**：

1. 展示任务列表、最近修改和验证摘要。
2. 切换时正确解绑旧事件。
3. 区分失败、等待确认、用户取消与完成。
4. 更新 TUI 操作指南并执行无提示体验检查。

**验收条件**：

- 切换后不串消息/成本/任务状态。
- 重启后明确哪些操作需重试。
- 人工能完成修复→测试→退出→继续流程。

**针对性验证**：go test -race -tags "sqlite memory" ./internal/tui ./cmd/basework -count=1；人工端到端记录。命令均从仓库根执行；必须记录结果和覆盖边界。

**交付与回填**：代码/文档 diff、必要测试，以及 `docs/development/evidence/UI-003.md`。更新看板的状态和证据；有用户可见行为变化时更新对应指南及 STATUS。

**范围外**：不顺手改其他任务；公共 API、存储格式、默认权限的额外变化必须先写明影响并拆分。
